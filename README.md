# human-compiler
This is a project where I attempt to be a compiler and write the assembly for a C program.  I will then add the compiler output after to assess how bad I am at manually converting C into assembly.
